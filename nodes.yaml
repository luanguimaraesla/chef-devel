boca:
  run_list:
    - role[boca_server]

squid:
  run_list:
    - role[squid_server]

redmine:
  run_list:
    - role[redmine_server]

polaris:
  run_list:
    - role[opennebula-frontend]
    - role[opennebula-node]
    - role[polaris] 

solarian:
  run_list:
    - role[opennebula-node]
    - role[solarian]

imperius:
  run_list:
    - role[opennebula-node]
    - role[imperius]

firewall:
  run_list:
    - role[firewall]	


