boca:
  run_list:
    - role[boca_server]

squid:
  run_list:
    - role[squid_server]

redmine:
  run_list:
    - role[redmine_server]

opennebula-frontend:
  run_list:
    - role[opennebula-frontend]

opennebula-node:
  run_list:
    - role[opennebula-node]
